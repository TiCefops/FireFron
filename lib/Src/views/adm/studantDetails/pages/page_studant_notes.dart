
import 'package:flutter/material.dart';
import 'package:get/get.dart';

class ViewInfoNotesStudant extends StatelessWidget {
  const ViewInfoNotesStudant({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SingleChildScrollView(
      child: Container(
        height: Get.height,
        color: Colors.red,

      ),
    );
  }
}
